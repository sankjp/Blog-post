/*import React from "react";

const Input=({type,name,placeholder,value,onchange})=>{
    return(
        <input type={type} className={name} placeholder={placeholder} onchange={onchange}/>
    );
};

export default Input;*/